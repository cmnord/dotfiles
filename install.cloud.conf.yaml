- defaults:
    link:
      create: true
      force: true
      relink: true

- link:
    ~/.dotfiles: ''
    ~/.gitignore_global:
    ~/.inputrc:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.zsh:
    ~/bin/:
      glob: true
      path: bin/*


- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    /usr/local/bin/diff-so-fancy: bin/diff-so-fancy
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.inputrc:
    ~/.oh-my-zsh/custom/plugins: zsh-plugins
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
